name: Supply chain

on:
  push:
    branches: [main]
    paths:
      - "**/Cargo.toml"
      - "**/Cargo.lock"
      - "deny.toml"
      - ".github/workflows/supply-chain.yml"
  # No `paths` filter here: the "Protect main" ruleset requires both cargo-deny
  # contexts on every PR, and a job that never runs never reports, so a path-filtered
  # trigger would leave any PR outside those paths blocked forever.
  pull_request:
    branches: [main]
  schedule:
    - cron: "17 5 * * 1"

jobs:
  cargo-deny:
    name: cargo-deny (${{ matrix.checks }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        checks:
          - advisories
          - bans licenses sources
    continue-on-error: ${{ matrix.checks == 'advisories' }}
    steps:
      - uses: actions/checkout@v7
      - uses: EmbarkStudios/cargo-deny-action@v2
        with:
          command: check ${{ matrix.checks }}
